<li>
	{{burger_name}}

	<button id="change-devoured" class="btn btn-light" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Ive been Devoured!{{else}}Eat Me!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
