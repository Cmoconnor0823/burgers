{{!-- Setup the index.handlebars to have the template 
that Handlebars can render onto. Create a button in 
index.handlebars that will submit the user input into the
database. --}}

<h1>Burgers Now!</h1>

<h2>Here's what's on the menu!</h2>

<ul>
    {{#each burgers}}
    {{#unless eaten}}
    {{> burger/burgerblock devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that are currently 86'ed</h2>

<ul>
    {{#each burgers}}
    {{#if eaten}}
    {{> burger/burgerblock devoured=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger to the Menu</h2>
<form class="create-form">

    <div class="form-group">
        <lable for="ca"> Burger Name:</lable>
        <input type="text" id="ca" burger_name="name">
    </div>

    <div class="form-group">
        <label for="eaten">Devoured?</label><br>
        <input type="radio" name="eaten" value="1" checked> This one's been devoured!<br>
        <input type="radio" name="eaten" value="0"> Eat Me!
    </div>

    <button type="submit">Add a Burger</button>
</form>